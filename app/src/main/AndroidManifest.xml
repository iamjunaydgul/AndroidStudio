<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.features_andapplications">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".eggTimer"></activity>
        <activity android:name=".weatherForecaster" />
        <activity android:name=".javaScriptObjectNotation" />
        <activity android:name=".guessTheCelebrity" />
        <activity android:name=".downloadingImages" />
        <activity android:name=".downloadingWebContent" />
        <activity android:name=".brainTrainer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>